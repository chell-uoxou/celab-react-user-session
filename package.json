{
  "name": "celab-react-user-session-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "package",
    "examples"
  ],
  "scripts": {
    "build:lib": "cd package && npm run build",
    "build:examples": "cd examples && npm run build",
    "dev": "cd examples && npm run dev"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "eslint": "^9",
    "typescript": "^5"
  }
}
